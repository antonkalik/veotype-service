export enum TransporterType {
  Gmail = 'gmail',
  AWS = 'aws',
}

export enum Extension {
  'png' = 'png',
  'jpg' = 'jpg',
  'jpeg' = 'jpeg',
}

export enum KeyboardType {
  US = 'US',
  UK = 'UK',
  ES = 'ES',
  LATAM = 'LATAM',
}

export enum Locale {
  en = 'en',
  es = 'es',
}
